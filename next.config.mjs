/** @type {import('next').NextConfig} */
const nextConfig = {
    // Allows importing images from the local folder if needed
    // Assuming NFTs are placed inside public/NFTs folder
};

export default nextConfig;
